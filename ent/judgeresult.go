// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ProductBuildersHQ/visionstudio/ent/judgeresult"
	"github.com/ProductBuildersHQ/visionstudio/ent/judgerubric"
)

// JudgeResult is the model entity for the JudgeResult schema.
type JudgeResult struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// InitiativeID holds the value of the "initiative_id" field.
	InitiativeID string `json:"initiative_id,omitempty"`
	// SpecPath holds the value of the "spec_path" field.
	SpecPath string `json:"spec_path,omitempty"`
	// Score holds the value of the "score" field.
	Score float64 `json:"score,omitempty"`
	// Rationale holds the value of the "rationale" field.
	Rationale string `json:"rationale,omitempty"`
	// Model holds the value of the "model" field.
	Model string `json:"model,omitempty"`
	// EvaluatedAt holds the value of the "evaluated_at" field.
	EvaluatedAt time.Time `json:"evaluated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the JudgeResultQuery when eager-loading is set.
	Edges                JudgeResultEdges `json:"edges"`
	judge_rubric_results *string
	selectValues         sql.SelectValues
}

// JudgeResultEdges holds the relations/edges for other nodes in the graph.
type JudgeResultEdges struct {
	// Rubric holds the value of the rubric edge.
	Rubric *JudgeRubric `json:"rubric,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// RubricOrErr returns the Rubric value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e JudgeResultEdges) RubricOrErr() (*JudgeRubric, error) {
	if e.Rubric != nil {
		return e.Rubric, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: judgerubric.Label}
	}
	return nil, &NotLoadedError{edge: "rubric"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*JudgeResult) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case judgeresult.FieldScore:
			values[i] = new(sql.NullFloat64)
		case judgeresult.FieldID, judgeresult.FieldInitiativeID, judgeresult.FieldSpecPath, judgeresult.FieldRationale, judgeresult.FieldModel:
			values[i] = new(sql.NullString)
		case judgeresult.FieldEvaluatedAt:
			values[i] = new(sql.NullTime)
		case judgeresult.ForeignKeys[0]: // judge_rubric_results
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the JudgeResult fields.
func (_m *JudgeResult) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case judgeresult.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case judgeresult.FieldInitiativeID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field initiative_id", values[i])
			} else if value.Valid {
				_m.InitiativeID = value.String
			}
		case judgeresult.FieldSpecPath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field spec_path", values[i])
			} else if value.Valid {
				_m.SpecPath = value.String
			}
		case judgeresult.FieldScore:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field score", values[i])
			} else if value.Valid {
				_m.Score = value.Float64
			}
		case judgeresult.FieldRationale:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field rationale", values[i])
			} else if value.Valid {
				_m.Rationale = value.String
			}
		case judgeresult.FieldModel:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field model", values[i])
			} else if value.Valid {
				_m.Model = value.String
			}
		case judgeresult.FieldEvaluatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field evaluated_at", values[i])
			} else if value.Valid {
				_m.EvaluatedAt = value.Time
			}
		case judgeresult.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field judge_rubric_results", values[i])
			} else if value.Valid {
				_m.judge_rubric_results = new(string)
				*_m.judge_rubric_results = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the JudgeResult.
// This includes values selected through modifiers, order, etc.
func (_m *JudgeResult) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryRubric queries the "rubric" edge of the JudgeResult entity.
func (_m *JudgeResult) QueryRubric() *JudgeRubricQuery {
	return NewJudgeResultClient(_m.config).QueryRubric(_m)
}

// Update returns a builder for updating this JudgeResult.
// Note that you need to call JudgeResult.Unwrap() before calling this method if this JudgeResult
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *JudgeResult) Update() *JudgeResultUpdateOne {
	return NewJudgeResultClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the JudgeResult entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *JudgeResult) Unwrap() *JudgeResult {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: JudgeResult is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *JudgeResult) String() string {
	var builder strings.Builder
	builder.WriteString("JudgeResult(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("initiative_id=")
	builder.WriteString(_m.InitiativeID)
	builder.WriteString(", ")
	builder.WriteString("spec_path=")
	builder.WriteString(_m.SpecPath)
	builder.WriteString(", ")
	builder.WriteString("score=")
	builder.WriteString(fmt.Sprintf("%v", _m.Score))
	builder.WriteString(", ")
	builder.WriteString("rationale=")
	builder.WriteString(_m.Rationale)
	builder.WriteString(", ")
	builder.WriteString("model=")
	builder.WriteString(_m.Model)
	builder.WriteString(", ")
	builder.WriteString("evaluated_at=")
	builder.WriteString(_m.EvaluatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// JudgeResults is a parsable slice of JudgeResult.
type JudgeResults []*JudgeResult
