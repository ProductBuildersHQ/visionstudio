// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ProductBuildersHQ/visionstudio/ent/initiative"
	"github.com/ProductBuildersHQ/visionstudio/ent/phase"
)

// Phase is the model entity for the Phase schema.
type Phase struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// SequenceNumber holds the value of the "sequence_number" field.
	SequenceNumber int `json:"sequence_number,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// Theme holds the value of the "theme" field.
	Theme string `json:"theme,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PhaseQuery when eager-loading is set.
	Edges             PhaseEdges `json:"edges"`
	initiative_phases *string
	selectValues      sql.SelectValues
}

// PhaseEdges holds the relations/edges for other nodes in the graph.
type PhaseEdges struct {
	// Initiative holds the value of the initiative edge.
	Initiative *Initiative `json:"initiative,omitempty"`
	// RoadmapItems holds the value of the roadmap_items edge.
	RoadmapItems []*RoadmapItem `json:"roadmap_items,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// InitiativeOrErr returns the Initiative value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PhaseEdges) InitiativeOrErr() (*Initiative, error) {
	if e.Initiative != nil {
		return e.Initiative, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: initiative.Label}
	}
	return nil, &NotLoadedError{edge: "initiative"}
}

// RoadmapItemsOrErr returns the RoadmapItems value or an error if the edge
// was not loaded in eager-loading.
func (e PhaseEdges) RoadmapItemsOrErr() ([]*RoadmapItem, error) {
	if e.loadedTypes[1] {
		return e.RoadmapItems, nil
	}
	return nil, &NotLoadedError{edge: "roadmap_items"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Phase) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case phase.FieldSequenceNumber:
			values[i] = new(sql.NullInt64)
		case phase.FieldID, phase.FieldTitle, phase.FieldTheme:
			values[i] = new(sql.NullString)
		case phase.ForeignKeys[0]: // initiative_phases
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Phase fields.
func (_m *Phase) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case phase.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case phase.FieldSequenceNumber:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sequence_number", values[i])
			} else if value.Valid {
				_m.SequenceNumber = int(value.Int64)
			}
		case phase.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				_m.Title = value.String
			}
		case phase.FieldTheme:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field theme", values[i])
			} else if value.Valid {
				_m.Theme = value.String
			}
		case phase.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field initiative_phases", values[i])
			} else if value.Valid {
				_m.initiative_phases = new(string)
				*_m.initiative_phases = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Phase.
// This includes values selected through modifiers, order, etc.
func (_m *Phase) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryInitiative queries the "initiative" edge of the Phase entity.
func (_m *Phase) QueryInitiative() *InitiativeQuery {
	return NewPhaseClient(_m.config).QueryInitiative(_m)
}

// QueryRoadmapItems queries the "roadmap_items" edge of the Phase entity.
func (_m *Phase) QueryRoadmapItems() *RoadmapItemQuery {
	return NewPhaseClient(_m.config).QueryRoadmapItems(_m)
}

// Update returns a builder for updating this Phase.
// Note that you need to call Phase.Unwrap() before calling this method if this Phase
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Phase) Update() *PhaseUpdateOne {
	return NewPhaseClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Phase entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Phase) Unwrap() *Phase {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Phase is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Phase) String() string {
	var builder strings.Builder
	builder.WriteString("Phase(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("sequence_number=")
	builder.WriteString(fmt.Sprintf("%v", _m.SequenceNumber))
	builder.WriteString(", ")
	builder.WriteString("title=")
	builder.WriteString(_m.Title)
	builder.WriteString(", ")
	builder.WriteString("theme=")
	builder.WriteString(_m.Theme)
	builder.WriteByte(')')
	return builder.String()
}

// Phases is a parsable slice of Phase.
type Phases []*Phase
