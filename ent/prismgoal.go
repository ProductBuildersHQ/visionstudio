// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ProductBuildersHQ/visionstudio/ent/prismgoal"
)

// PRISMGoal is the model entity for the PRISMGoal schema.
type PRISMGoal struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Organization holds the value of the "organization" field.
	Organization string `json:"organization,omitempty"`
	// RepositoryID holds the value of the "repository_id" field.
	RepositoryID string `json:"repository_id,omitempty"`
	// GoalType holds the value of the "goal_type" field.
	GoalType string `json:"goal_type,omitempty"`
	// Document holds the value of the "document" field.
	Document map[string]interface{} `json:"document,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PRISMGoal) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case prismgoal.FieldDocument:
			values[i] = new([]byte)
		case prismgoal.FieldID, prismgoal.FieldOrganization, prismgoal.FieldRepositoryID, prismgoal.FieldGoalType:
			values[i] = new(sql.NullString)
		case prismgoal.FieldCreatedAt, prismgoal.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PRISMGoal fields.
func (_m *PRISMGoal) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case prismgoal.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case prismgoal.FieldOrganization:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field organization", values[i])
			} else if value.Valid {
				_m.Organization = value.String
			}
		case prismgoal.FieldRepositoryID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field repository_id", values[i])
			} else if value.Valid {
				_m.RepositoryID = value.String
			}
		case prismgoal.FieldGoalType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field goal_type", values[i])
			} else if value.Valid {
				_m.GoalType = value.String
			}
		case prismgoal.FieldDocument:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field document", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Document); err != nil {
					return fmt.Errorf("unmarshal field document: %w", err)
				}
			}
		case prismgoal.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case prismgoal.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PRISMGoal.
// This includes values selected through modifiers, order, etc.
func (_m *PRISMGoal) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this PRISMGoal.
// Note that you need to call PRISMGoal.Unwrap() before calling this method if this PRISMGoal
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *PRISMGoal) Update() *PRISMGoalUpdateOne {
	return NewPRISMGoalClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the PRISMGoal entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *PRISMGoal) Unwrap() *PRISMGoal {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: PRISMGoal is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *PRISMGoal) String() string {
	var builder strings.Builder
	builder.WriteString("PRISMGoal(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("organization=")
	builder.WriteString(_m.Organization)
	builder.WriteString(", ")
	builder.WriteString("repository_id=")
	builder.WriteString(_m.RepositoryID)
	builder.WriteString(", ")
	builder.WriteString("goal_type=")
	builder.WriteString(_m.GoalType)
	builder.WriteString(", ")
	builder.WriteString("document=")
	builder.WriteString(fmt.Sprintf("%v", _m.Document))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PRISMGoals is a parsable slice of PRISMGoal.
type PRISMGoals []*PRISMGoal
